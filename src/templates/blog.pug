extend ./layout.pug
block content
  each post in posts
    section.post
      .container
        .row
          .col-lg-8.col-lg-offset-2.text-center
            h1.section-heading #{post.title}
            hr.light
            p.faded #{post.description}
            a(href='/blog/'+ post.title).btn.btn-default Read more
